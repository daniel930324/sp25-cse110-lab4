1. Line 9 will print 20.
2. Line 13 will also print 20.
3. We should not use var because it's easier to cause naming conflicts and scoping issues
4. Line 9 will print 20.
5. Line 13 returns an error the variable 'result' is not defined in the scope that line 13 is at.
6. Line 9 returns error because the we were attempting to re-assign values to a const variable at line 7
7. Line 13 returns an error the variable 'result' is not defined in the scope that line 13 is at.